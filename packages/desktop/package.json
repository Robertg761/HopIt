{
  "name": "@hopit/desktop",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "HopIt menu-bar desktop app: a thin shell over the local hop agent.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test test/*.test.js",
    "package": "node scripts/package-desktop.mjs"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.2",
    "electron": "^43.1.1"
  }
}
